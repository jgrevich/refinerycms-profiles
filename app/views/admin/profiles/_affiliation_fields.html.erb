<div class='field'>
    <%= f.select :organization_id, ("<option value='' disabled='disabled'>Organization</option>" + options_from_collection_for_select(ProfileOrganization.all, :id, :name, f.object.organization_id)) %>
    <%= f.select :department_id, ("<option value='' disabled='disabled'>Department</option>" + options_from_collection_for_select(ProfileDepartment.all, :id, :name, f.object.department_id)) %>
    <%= f.select :title_id, ("<option value='' disabled='disabled'>Title</option>" + options_from_collection_for_select(ProfileTitle.all, :id, :name, f.object.title_id)) %>
    
    <%= link_to_function "remove", "remove_fields(this) "%>
</div>